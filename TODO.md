# CafeFlow — งานที่เหลือ

> อัปเดต 19 ก.ย. 2569 · 15 commit · สำรวจจากโค้ดจริง ไม่ใช่จากความจำ
> เปิดไฟล์นี้ก่อนเริ่มงานรอบหน้า แล้วอัปเดตเมื่อปิดงานแต่ละข้อ

---

## ตอนนี้อยู่ตรงไหน

ร้าน **เปิด-ปิดวันได้ครบวงจรบนฐานข้อมูลจริง** แล้ว

| ทำได้แล้ว | หมายเหตุ |
|---|---|
| ขายครบวง เงินสด + QR พร้อมเพย์ผูกยอด | QR สร้างเองในเครื่อง ไม่ต้องต่อ gateway |
| พิมพ์จริง ESC/POS สลิปครัว + ใบเสร็จ | ส่งเป็นภาพ แก้ปัญหาภาษาไทยเพี้ยนถาวร |
| ปิดรอบ + ควบคุมเงินสด | expected_cash คิดจากฐาน ไม่เชื่อหน้าจอ |
| แก้เมนู + อัปรูปสินค้าจากเครื่อง | ย่อรูปอัตโนมัติ · ชื่อไฟล์เป็น sha256 |
| รายงานข้ามวัน + ค้นออเดอร์ย้อนหลัง | คิดด้วย SQL ที่เซิร์ฟเวอร์ |
| ล็อกอินจริง + สิทธิ์ตรวจทุก endpoint | argon2 · cookie httpOnly |
| จับคู่อุปกรณ์ (คีออสก์/จอคิว) | รหัส 6 หลัก อายุ 10 นาที |
| จอแสดงคิว 60/40 + เสียงเรียก | §22 §36 |
| สำรอง + กู้คืน + สำเนานอกเครื่อง | **ซ้อมกู้จริงแล้ว** กู้ได้ใน 12 วินาที |

9 หน้าเว็บ · 37 endpoint · 2 migration · หน้าเว็บทำงานกับเซิร์ฟเวอร์อย่างเดียว (ตัดโหมดเดโม localStorage ทิ้งแล้ว)

**ยังไม่เคยเปิดร้านจริง** — ดูหัวข้อ E ก่อนเปิดใช้งาน

---

## A · ต้องมีของจริงก่อน (ทำแทนไม่ได้)

ข้อพวกนี้ **บล็อกการเปิดร้าน** และต้องจัดหาของก่อน ไม่ใช่งานเขียนโค้ด

- [ ] **A1 · ทดสอบเครื่องพิมพ์ความร้อนตัวจริง**
  ทดสอบผ่านเครื่องจำลองที่ถอด raster กลับมาดูเป็นภาพแล้ว แต่ยังไม่เคยเข้าเครื่องจริง
  บางรุ่นตีความคำสั่ง `GS v 0` ต่างออกไป — **ลองพิมพ์ใบแรกก่อนเปิดร้าน**
  ถ้าไม่ผ่าน: ปรับ `api/src/print/escpos.js` (ลองลดความสูงแถบจาก 255 ลง)
  ตั้งเครื่องได้จากหน้าภาพรวม › เครื่องพิมพ์ แล้ว (LAN หรือ USB) · ทางส่ง USB บน Windows
  ทดสอบแล้วแค่กรณีชื่อผิด ยังไม่เคยส่งเข้าเครื่องจริง

- [ ] **A2 · ทดสอบ QR กับบัญชีพร้อมเพย์จริง**
  payload ผ่านการตรวจโครง EMVCo และ CRC แล้ว (8 เทสต์ใน `api/test/emv.test.js`)
  แต่ยังไม่เคยมีเงินเข้าจริง — ออก QR 1 บาท สแกนด้วยแอป 3 ธนาคาร
  ตั้งพร้อมเพย์ที่ **หน้าภาพรวม › ตั้งค่าคีออสก์ › พร้อมเพย์ของร้าน**

- [ ] **A3 · ยืนยันชื่อสินค้าที่อ่านจากป้ายไม่ออก**
  `api/src/db/seed-data.js` มี `// TODO` — รายการใน RECOMMENDED MENU ที่ชื่อถูกบังบนรูปป้าย
  ใส่ไว้ชั่วคราวเป็น "ชาไทย CLOUD" · ต้องให้เจ้าของร้านดูป้ายจริง

- [ ] **A4 · เก็บสลิปโอนเงินจริง 100 ใบ** (ถ้าจะทำ OCR — ดู D1)
  จากธนาคารที่ลูกค้าร้านใช้จริง เอามาวัดความแม่นก่อนลงแรง

- [ ] **A5 · รันจริงทั้งวัน** ดูว่ามี memory leak หรือ connection รั่วไหม

---

## B · ความปลอดภัยที่ยังเหลือ

ช่องใหญ่ปิดไปแล้ว (จับคู่อุปกรณ์ · สิทธิ์ทุก endpoint · `/api/bootstrap`)
ที่เหลือเป็นงานดูแลระยะยาว ไม่ใช่ช่องที่ใช้ได้ทันที

- [ ] **B1 · ล้าง session ที่หมดอายุ**
  แถวใน `session` ค้างสะสมตลอด ไม่มี job ลบ
  ทำ: job ใน `api/src/server.js` แบบเดียวกับ `expireDueQr` ใน `routes/payments.js`

- [ ] **B2 · rate limit**
  ตอนนี้มีเฉพาะการหน่วงตอนล็อกอินผิด กับการล็อกตอนกรอกรหัสจับคู่ผิด
  endpoint อื่นยิงรัวได้ไม่จำกัด · ใช้ `@fastify/rate-limit`

- [ ] **B3 · partition + retention ของ `audit_log`**
  ตารางนี้โตเร็วที่สุด (ทุกการเปลี่ยนสถานะเขียนหนึ่งแถว) ยังไม่มีแผนตัดเก่าทิ้ง

---

## C · ไฮบริดที่ยังทำครึ่งเดียว

แผนวางไว้ว่า "ร้านเป็นแหล่งความจริง คลาวด์เป็นสำเนา" — ตอนนี้มีแต่ฝั่งร้าน

- [ ] **C1 · sync-worker + cloud ingest**
  ตาราง `outbox` สร้างไว้แล้วใน `001_initial.sql` แต่**ยังไม่มีใครเขียนหรืออ่าน**
  (จงใจ — เขียนก่อนมีตัวเก็บจะได้แค่แถวกองสะสม ดูคอมเมนต์ที่ `routes/orders.js` ฟังก์ชัน `touch()`)

  ทำสามอย่างพร้อมกัน:
  1. `touch()` เขียน `outbox` ในทรานแซกชันเดียวกับข้อมูล
  2. `sync-worker` อ่าน outbox → `POST /ingest/v1/:branch` พร้อม `Idempotency-Key`
  3. ฝั่งคลาวด์ upsert ตาม `(branch, entity, entity_id)` เอา `row_rev` มากกว่าเท่านั้น
     **ห้ามใส่ FK ฝั่งคลาวด์** ไม่งั้น batch ที่ลูกมาก่อนพ่อจะล้มแล้ว retry วนไม่จบ

  ★ ซิงก์**ทางเดียว** ร้าน→คลาวด์ ทุกแถวมีผู้เขียนคนเดียว จึงไม่มี merge conflict
  ให้ต้องแก้เลย — อย่าเผลอทำสองทาง

- [ ] **C2 · WAL archiving** (`pg_receivewal`)
  ตอนนี้ **RPO ~1 ชั่วโมง** — ข้อมูลระหว่างสำรองครั้งล่าสุดถึงตอนพังหายจริง

- [ ] **C3 · เครื่องสำรองพร้อมใช้** — mini PC เครื่องสองที่ลง image เดียวกัน (เรื่องจัดซื้อ)

- [ ] **C4 · ตั้ง cron ของ backup/offsite จริงบนเครื่องร้าน**
  สคริปต์พร้อมแล้ว (`ops/backup.sh`, `ops/offsite.sh`) ตาราง cron อยู่ใน `ops/README.md`
  **แต่ยังไม่ได้ตั้งจริง** — และต้องเสียบ USB หรือตั้ง rclone ก่อน

---

## D · ฟีเจอร์ตามสเปกที่ยังไม่มี

- [x] **D1a · สแกน QR บนสลิปที่คีออสก์ (ไม่ใช้เน็ต)** — กันสลิปซ้ำ แล้วส่งไปรอแคชเชียร์ยืนยัน
  ตัวอ่านอยู่ที่ `shared/cf-slip.js` · ตั้งเครื่องคีออสก์ดู `ops/README.md` หัวข้อ "กล้องสแกนสลิป"
  **ยังไม่ได้ทดสอบกับสลิปจริงของธนาคาร** — โครง QR ทำตามมาตรฐาน Slip Verification ต้องลองสแกน
  สลิปจริงจากหลายธนาคารก่อนใช้ ถ้าบางธนาคารอ่านไม่ผ่าน ให้เก็บ payload มาปรับ `CFSlip.parse`
- [ ] **D1b · ตรวจสลิปกับบริการออนไลน์** — ถ้าผ่าน (ยอดตรง · บัญชีร้าน · ref ไม่ซ้ำ) ส่งเข้าครัวได้เลย
  จุดที่ต่อ: `submitSlip()` ใน `api/src/routes/payments.js` · เน็ตหลุดต้องถอยกลับเป็นแคชเชียร์ยืนยัน
- [x] **D1 · OCR อ่านยอด/วันที่จากภาพสลิป** — PaddleOCR ใน `ocr-svc/` · กฎอยู่ `shared/cf-slip-rules.js`
  วัดกับสลิปจริง 3 ใบ (กสิกร K+/make · กรุงไทย): ยอดถูก 3/3 · วันที่ถูก 2/3 (ภาพเบลอ 1 ใบอ่านไม่ออก → เทา ไม่เตือนแดง)
  ยังต้องเก็บสลิปจริงเพิ่ม 20–30 ใบหลายธนาคาร (ข้อ A4) แล้วเติมเคสลง `api/test/slip-rules.test.js`
- [ ] **สำรองโฟลเดอร์ `data/`** (ภาพสลิป + รูปสินค้า) — `ops/backup.sh` ยังสำรองแค่ฐานข้อมูล
  โครงรองรับครบแล้ว: ตาราง `payment_slip` มีทุกคอลัมน์ · `PAYMENT_REVIEW` อยู่ในผังสถานะ
  · หน้าแคชเชียร์มีการ์ด "ข้อมูลที่อ่านได้จากสลิป" พร้อมช่องเหตุผลอยู่แล้ว
  **OCR แค่เติมข้อมูลลงการ์ดที่มีอยู่ ไม่ต้องแตะ state machine**

  แผนที่วางไว้: service Python แยก (`ocr-svc`) · **อ่านบาร์โค้ดบนสลิปก่อน แล้วค่อย OCR
  ข้อความ** (สลิปไทยส่วนใหญ่มี ref ฝังในบาร์โค้ด แม่นเกือบ 100% ส่วนตัวเลขจาก OCR ไม่แม่น)
  · กฎตรวจ 4 ข้อ (ยอดตรง · เวลาอยู่ในกรอบ · เวลาโอน ≥ เวลาออก QR · ref ไม่ซ้ำ)
  ให้สร้างเป็น `shared/cf-slip-rules.js` **ไฟล์นี้ยังไม่มี** — จะได้ใช้ข้อความเดียวกันทั้งสองฝั่ง
  · **OCR ห้ามทำให้ออเดอร์เป็น PAID เอง** ต้องมีคนกดยืนยัน
  · ถ้า ocr-svc ล่ม ระบบต้องถอยกลับเป็นกรอกมือได้เอง

  ⚠️ ถ้าความแม่นต่ำกว่า 85% ให้ **ตัดทิ้งไปก่อน** แล้วขายด้วย QR เฉย ๆ

- [ ] **D2 · CMS โฆษณา (§23)**
  จอแสดงคิวหมุนเมนูแนะนำแทนอยู่ตอนนี้ · ถ้าจะทำจริงต้องมีที่อัปไฟล์ + ตารางเวลาแสดง
  (`/api/media/upload` ใช้ซ้ำได้)

- [ ] **D3 · หน้าเพิ่ม/แก้ผู้ใช้ (§29)**
  ตอนนี้ดูรายชื่อได้อย่างเดียว · ยังเพิ่มคน เปลี่ยนบทบาท หรือรีเซ็ตรหัสไม่ได้
  ต้องทำก่อนส่งมอบจริง ไม่งั้นเปลี่ยนพนักงานทีต้องแก้ฐาน

- [ ] **D4 · เสียงพูดภาษาไทย (§31)**
  ตอนนี้จอคิวมีแค่เสียงกริ่ง · สเปกอยากได้เสียงพูด 5 แบบ

---

## E · หลายสาขา (Phase 4 ของสเปก)

ยังไม่ต้องทำ แต่**โครงรองรับไว้แล้ว** — `branch_id` อยู่ทุกตารางตั้งแต่ migration แรก
เมื่อถึงเวลาต้องตัดสินใจ: ต่อสาขาให้เมนูถูกจัดการจาก `STORE` หรือ `CLOUD` ฝั่งเดียว
**ห้ามให้แก้ได้ทั้งสองทาง** ไม่งั้นเจอ split-brain ราคา (ลูกค้าเห็นราคาหนึ่ง บิลออกอีกราคา)

---

## ลำดับที่แนะนำ

1. **A1 + A2** ทันทีที่ได้เครื่องพิมพ์กับบัญชี — ถ้าเครื่องพิมพ์ไม่ผ่านต้องรู้ก่อนลงแรงอย่างอื่น
2. **C4** ตั้ง cron จริง (ครึ่งวัน) — ของพร้อมแล้ว เหลือแค่ตั้ง
3. **D3** หน้าจัดการผู้ใช้ — จำเป็นก่อนส่งมอบ
4. **B1 + B2** งานดูแลระยะยาว
5. **C1** ซิงก์คลาวด์ เมื่อร้านเดินนิ่งแล้ว
6. **D1** OCR เมื่อมีสลิปจริงมาวัด

---

## กติกาที่ต้องรักษา (อ่านก่อนแก้โค้ด)

รายละเอียดเต็มอยู่ใน [`app/README.md`](app/README.md) — ที่พลาดบ่อยที่สุด:

1. **`design-system-2/` ห้ามแก้แม้แต่ไบต์เดียว** — ยืนยันด้วย
   `diff -r design-system/design-system design-system-2` ต้องเงียบ
2. **ตรรกะที่เซิร์ฟเวอร์กับเบราว์เซอร์ต้องคิดตรงกันอยู่ใน `shared/`** (ผังสถานะ · สูตรราคา ·
   กฎตัวเลือก · ตารางสิทธิ์) — คัดลอกไปอีกฝั่งเมื่อไหร่ มันจะเพี้ยนจากกันเสมอ
3. **ราคาเป็น sparse map** — คีย์ที่ไม่มี = ไม่ขายแบบนั้น **ไม่ใช่ราคา 0**
   (ราคา 0 แปลว่าแจกฟรี คนละเรื่องกัน)
4. **สลิปครัวสะกดตัวเลือกเต็มคำเสมอ** แม้กระดาษจะแคบ — ครัวใช้ตัดสินใจผลิต
5. `cf-kiosk.css` **ห้ามมี `@media (max-width:…)`** (เวทีถูก letterbox ค่าที่วัดได้จะโกหก)
   — แต่ `cf-display.css` ใช้ได้ เพราะจอคิวกินทั้งหน้าต่างจริง

### ตรวจก่อน commit ทุกครั้ง

```bash
cd api && npm test                              # 8 เทสต์ (EMV payload)
diff -r design-system/design-system design-system-2
```

---

## เริ่มงานใหม่ยังไง

```bash
docker compose up -d db
cd api && npm install && npm run migrate && npm run seed   # ครั้งแรกเท่านั้น
npm start                                                   # → http://localhost:8080/app/login.html
```

การดูแลระบบหน้าร้าน (สำรอง กู้คืน เครื่องพิมพ์ ปัญหาหน้างาน): [`ops/README.md`](ops/README.md)
