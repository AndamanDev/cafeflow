/**
 * CafeFlow — ค่าตั้งของ "หน้าเว็บชุดนี้" (ไม่ใช่ค่าตั้งของร้าน)
 * ══════════════════════════════════════════════════════════════════
 * ไฟล์นี้บอกว่าหน้าเว็บควรอ่านข้อมูลจากไหน
 *
 *   local  ข้อมูลอยู่ใน localStorage ของเบราว์เซอร์เครื่องนี้ (เดโม/ออฟไลน์)
 *   api    ข้อมูลจริงอยู่ที่เซิร์ฟเวอร์ในร้าน
 *
 * ค่าในไฟล์นี้คือ local เสมอ — เพราะถ้าเปิดหน้าเว็บด้วย static server เฉย ๆ
 * (python -m http.server หรือดับเบิลคลิกไฟล์) ก็ต้องยังเดินได้
 *
 * **เซิร์ฟเวอร์ในร้านจะเสิร์ฟไฟล์นี้ทับด้วยรุ่นที่ตั้งเป็น api**
 * หน้าเว็บจึงเลือกหลังบ้านถูกเองตามว่าใครเป็นคนเสิร์ฟมัน โดยไม่ต้องพก
 * query string ไปทุกลิงก์ (ซึ่งหายทันทีที่มีการ redirect ไปหน้าล็อกอิน)
 */
window.CF_BACKEND = 'local';
